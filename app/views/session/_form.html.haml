= form_with url: sign_in_path, local: true do |f|
  %div
    = f.label :email
    = f.text_field :email
  %div
    = f.label :password
    = f.password_field :password
  %div
    = f.submit 'sign in'
